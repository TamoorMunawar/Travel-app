import 'package:flutter/cupertino.dart';

class BAritemPage extends StatelessWidget {
  const BAritemPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Bar Ite Page"),
      ),
    );
    
  }
}